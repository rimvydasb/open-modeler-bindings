# Open Modeler Demo Projects

This directory contains standalone domain model prototypes that showcase the reactivity framework and execution engine.

## How to Run Demos

Generally, you can run them from the project root:

```bash
# Loan Schedule
deno run --allow-read loan-schedule/main.ts
```

For more details on a specific demo, navigate to its directory.
